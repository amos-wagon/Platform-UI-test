# Aspentech Design System

Tokens and CSS defining the style and color palette of Aspentech web applications.

## Storybook
Components are demonstrated in Storybook. An instance will be deployed internally once the pipelines are ready.
Storybook can be started locally with npm run storybook.
